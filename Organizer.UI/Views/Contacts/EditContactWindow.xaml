<Window x:Class="Organizer.UI.Views.EditContactWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Organizer.UI.Views"
        xmlns:valid="clr-namespace:Organizer.UI.ValidationRules"
        mc:Ignorable="d"
        Title="EditContactWindow" Height="706.14" Width="1018.364"
        ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="{Binding EditContactHeader, FallbackValue='Edit contact data'}"
                       TextAlignment="Center"
                       Padding="7"
                       FontSize="20"
                       FontFamily="Comic Sans MS"
                       FontStyle="Italic" />
        </StackPanel>

        <StackPanel Grid.Row="1">
            <Grid Margin="5 15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock
                       Grid.Column="0"
                       FontFamily="Comic Sans MS"
                       FontSize="14"
                       Text="Основной телефон:"
                       Padding="5"
                       TextAlignment="Justify"
                       VerticalAlignment="Center" />

                <TextBox
                       Grid.Column="1"
                       FontFamily="Comic Sans MS"
                       FontSize="14"
                       Padding="5"
                       TextAlignment="Justify"
                       VerticalAlignment="Center">
                    <TextBox.Text>
                        <Binding Path="PrimaryPhone" NotifyOnValidationError="True">
                            <Binding.ValidationRules>
                                <valid:PhoneValidationRule />
                            </Binding.ValidationRules>
                        </Binding>
                    </TextBox.Text>
                </TextBox>
            </Grid>

            <Grid Margin="5 15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock
                       Grid.Column="0"
                       Text="Имя:"
                       FontFamily="Comic Sans MS"
                       FontSize="14"
                       Padding="5"
                       TextAlignment="Justify"
                       VerticalAlignment="Center" />

                <TextBox
                       Grid.Column="1"
                       FontFamily="Comic Sans MS"
                       FontSize="14"
                       Padding="5"
                       TextAlignment="Justify"
                       VerticalAlignment="Center">
                    <TextBox.Text>
                        <Binding Path="FirstName" NotifyOnValidationError="True">
                            <Binding.ValidationRules>
                                <valid:StringLengthValidationRule MinLength="2" MaxLength="20" />
                            </Binding.ValidationRules>
                        </Binding>
                    </TextBox.Text>
                </TextBox>
            </Grid>

            <Grid Margin="5 15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock
                       Grid.Column="0"
                       FontFamily="Comic Sans MS"
                       FontSize="14"
                       Padding="5"
                       Text="По-отчеству:"
                       TextAlignment="Justify"
                       VerticalAlignment="Center" />
                <TextBox
                       Grid.Column="1"
                       FontFamily="Comic Sans MS"
                       FontSize="14"
                       Padding="5"
                       TextAlignment="Justify"
                       VerticalAlignment="Center">
                    <Binding Path="MiddleName" NotifyOnValidationError="True">
                        <Binding.ValidationRules>
                            <valid:StringLengthValidationRule MinLength="5" MaxLength="25" />
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox>
            </Grid>

            <Grid Margin="5 15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Row="3"
                       Grid.Column="0"
                       Text="Фамилия:"
                       FontFamily="Comic Sans MS"
                       Padding="5"
                       FontSize="16"
                       TextAlignment="Justify"
                       VerticalAlignment="Center" />
                <TextBox
                       Grid.Column="1"
                       FontFamily="Comic Sans MS"
                       FontSize="14"
                       Padding="5"
                       TextAlignment="Justify"
                       VerticalAlignment="Center">
                    <Binding Path="LastName" NotifyOnValidationError="True">
                        <Binding.ValidationRules>
                            <valid:StringLengthValidationRule MinLength="2" MaxLength="25" />
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox>
            </Grid>

            <Grid Margin="5 15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Row="4"
                       Grid.Column="0"
                       Text="Никнейм:"
                       Padding="5"
                       FontFamily="Comic Sans MS"
                       FontSize="14"
                       TextAlignment="Justify"
                       VerticalAlignment="Center" />
                <TextBox
                       Grid.Column="1"
                       FontFamily="Comic Sans MS"
                       FontSize="14"
                       Padding="5"
                       TextAlignment="Justify"
                       VerticalAlignment="Center">
                    <Binding Path="NickName" NotifyOnValidationError="True">
                        <Binding.ValidationRules>
                            <valid:StringLengthValidationRule MinLength="3" MaxLength="16" />
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox>
            </Grid>

            <Grid Margin="5 15">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Row="5"
                       Grid.Column="0"
                       Text="Е-мейл:"
                       Padding="5"
                       FontFamily="Comic Sans MS"
                       FontSize="14"
                       TextAlignment="Justify"
                       VerticalAlignment="Center" />
                <TextBox
                       Grid.Column="1"
                       FontFamily="Comic Sans MS"
                       FontSize="14"
                       Padding="5"
                       TextAlignment="Justify"
                       VerticalAlignment="Center">
                    <Binding Path="Email" NotifyOnValidationError="True">
                        <Binding.ValidationRules>
                            <valid:EmailValidationRule />
                        </Binding.ValidationRules>
                    </Binding>
                </TextBox>
            </Grid>
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="0.2*" />
            </Grid.ColumnDefinitions>
            <DataGrid Grid.Column="0" ItemsSource="{Binding Socials, Mode=OneWay}"
                       FontSize="14"
                      SelectedItem="{Binding SelectedSocial}"
                      AutoGenerateColumns="False">
                <DataGrid.Columns>
                    <DataGridTextColumn
                        Header="App name"
                        Width="*"
                        Binding="{Binding AppName}" />
                    <DataGridTextColumn
                        Header="App id"
                        Width="*"
                        Binding="{Binding AppId}" />
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Column="1"
                        Orientation="Vertical"
                        VerticalAlignment="Center">
                <Button Content="{Binding RelativeSource={RelativeSource Self}, Path=Command.Text, FallbackValue='Add social'}"
                        Command="{Binding AddSocialCommand}"
                       FontSize="14"
                        Margin="10"
                        Padding="10" />

                <Button
                        Content="{Binding RelativeSource={RelativeSource Self}, Path=Command.Text, FallbackValue='Edit social'}"
                        Command="{Binding EditSocialCommand}"
                       FontSize="14"
                        Margin="10"
                        Padding="10" />

                <Button
                        Content="{Binding RelativeSource={RelativeSource Self}, Path=Command.Text, FallbackValue='Delete social'}"
                        Command="{Binding RemoveSocialCommand}"
                       FontSize="14"
                        Margin="10"
                        Padding="10" />
            </StackPanel>
        </Grid>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button
                FontFamily="Comic Sans MS"
                       FontSize="16"
                Height="30"
                Margin="30 10"
                Command="{Binding SaveCommand}"
                Content="{Binding RelativeSource={RelativeSource Self}, Path=Command.Text, FallbackValue='Save'}" />

            <Button Grid.Column="1"
                FontFamily="Comic Sans MS"
                       FontSize="16"
                Height="30"
                Margin="30 10"
                Command="{Binding CancelCommand}"
                Content="{Binding RelativeSource={RelativeSource Self}, Path=Command.Text, FallbackValue='Cancel'}" />
        </Grid>
    </Grid>
</Window>